m4_ifdef([AC_LANG_SOURCE],[
AC_DEFUN([FE_COMPILE_IFELSE],[
AC_COMPILE_IFELSE([AC_LANG_SOURCE([$1])],[$2],[$3])
])
AC_DEFUN([FE_LINK_IFELSE],[
AC_LINK_IFELSE([AC_LANG_SOURCE([$1])],[$2],[$3])
])
],[
AC_DEFUN([FE_COMPILE_IFELSE],[
AC_COMPILE_IFELSE([$1],[$2],[$3])
])
AC_DEFUN([FE_LINK_IFELSE],[
AC_LINK_IFELSE([$1],[$2],[$3])
])
])
